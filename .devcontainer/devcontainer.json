{
  "name": "centmail",
  "dockerComposeFile": "../dev/docker-compose.yml",
  "service": "backend",
  "workspaceFolder": "/app",
  "forwardPorts": [9000],
  "postStartCommand": "make dist && ./centmail --install --idempotent --yes --config dev/config.toml"
}
